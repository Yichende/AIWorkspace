/**
 * 图标配色 — 直接引用 CSS 变量（定义见 app.scss），
 * 随 .theme-light / .theme-dark 自动切换，消费方无需感知主题。
 */
export const IconColors = {
  primary: 'var(--color-primary)',
  secondary: 'var(--color-secondary)',
  accent: 'var(--color-accent)',
}
