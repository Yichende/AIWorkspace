# 配置文档参考 https://taro-docs.jd.com/docs/next/env-mode-config
# 只有 TARO_APP_ 前缀的变量会被注入构建产物（Taro 原生机制）。
# 这些值不含密钥，可以入库；不要在此放任何机密。
#
# 后端 API 基地址（含协议与端口，不要以 / 结尾）
TARO_APP_API_BASE="http://localhost:3000"
# TARO_APP_ID="开发环境下的小程序 AppID"
