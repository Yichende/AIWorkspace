{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".npmrc", "tsconfig.base.json", "pnpm-workspace.yaml"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "lint:fix": {},
    "type-check": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
